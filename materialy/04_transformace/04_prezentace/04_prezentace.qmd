---
title: "Indexace"
subtitle: "Zpracovávání výzkumných dat pomocí jazyka R"
title-slide-attributes:
  data-background-color: rgb(233, 84, 32)
format:
  revealjs:
    self-contained: true
    theme: default
    scrollable: true
engine: knitr
filters:
  - webr
webr:
  packages:
    - "dplyr"
    - "readxl"
    - "readr"
execute:
  echo: true
  warning: false
  cache: true
---

## Program

::: incremental
- opakování z minulé hodiny
- kontrola domácího úkolu
- nahrávání dat
- pipes
- chybějící hodnoty
- faktory
- změna datatypů/struktur
- balíček `dplyr`
  - filtrování
  - balíček `dplyr`
- chybějící hodnoty
:::

------------------------------------------------------------------------

## Kontrola domácí práce

### Úloha 1

::: incremental
1.  vytvoření RProjektu

2.  nový R skript

3.  nahrání uložených dat

4.  výpočet toho, kolik zaměstnanců s daným platem jde nabrat na specifikovanou dobu měsíců

5.  A nešlo by to jinak?
:::

------------------------------------------------------------------------

### Úloha 2

::: incremental
1.  
:::

## Indexace / výběr specifických dat

**Výběr specifických dat je jedna ze základních funkcí R**; není pro to nutně třeba potřeba používat balíčky. Balíčky nám celý proces ovšem značně usnadní.

### Vyhledávání pomocí \[ \] {#vyhledávání-pomocí}

> `objekt ve kterém se vyhledává[pozice či charakteristiky vyhledávané hodnoty ve všech dimenzích]`

- objekt o jedné dimenzi: `objekt[informace pro výběr hodnoty]`

- objekt o dvou dimenzích: `objekt[řádek, sloupec]`

- objekt o třech dimenzích: `objekt[řádek, sloupec, matice]`

- ...

------------------------------------------------------------------------

#### Vektory

Jak víme, vektory mají pouze jednu dimenzi. Proto k definování pozice hodnot(y) stačí pouze jeden údaj.

```{r}
vektor <- c(a = 558, b = 67846, c = 8797)

vektor[3] #treti pozice ve vektoru
vektor["c"] #hodnota pojmenovaná jako c
```

Proč se u jména hodnoty používají uvozovky?

::: fragment
Bez použití uvozovek bychom R říkali, ať vektor indexuje pomocí informací uložených v objektu `c`. I to je ovšem možné; **objekty samotné v sobě mohou obsahovat informace v potřebném formátu**.

```{r}
cislo <- 2
pismeno <- "a"
vektor[cislo]
vektor[pismeno]
```
:::

------------------------------------------------------------------------

**Je možné vybírat i více hodnot najednou**. Jakým způsobem byste vybrali první a třetí hodnotu z objektu `vektor`?

```{webr-r}

```

::: fragment
```{r}
vektor[c(1, 3)] #porad do vektoru vkladame pouze jeden "objekt"
#nepouzivame carku!
```

Na základě tohoto principu lze hodnoty vybírat pomocí různých způsobů:

```{r}
vektor[1:2] #prvni az druha hodnota
vektor[-1] #vsechny hodnoty krome prvni (pozor: lze jen s cisly!)
```
:::

------------------------------------------------------------------------

##### Lze vybírat i pomocí charakteristik jednotlivých hodnot?

Nyní dokážeme vybrat hodnoty podle čísla jejich pozice. Co když chceme ale vybrat hodnoty podle jejich charakteristik?

```{r}
identical(vektor[1], vektor[vektor < 600])
#proc jsou tyto hodnoty identicke?
```

`vektor[vektor < 600]` = vyber z objektu vektor ty hodnoty, které splňují podmínku, že jsou menší než 600

```{r}
vektor < 600 #pouze a je TRUE
#vektor[vektor < 600]) je proto defakto vektor["a"]
```

Proč nemůžeme jednoduše napsat `vektor[< 600]`?

:::: fragment
Při `< 600` **chybí levá stran**a; co je menší než 600?

::: callout-important
## Pro výběru se používají čistě logické hodnoty!

Výběr defacto znamená, že se **R předá vektor s hodnotami TRUE/FALSE** o tom, zda má být hodnota na dané pozici do výběru zařazena.

```{r}
vektor < 600 #TRUE, FLASE, FALSE
vektor[vektor < 600]
vektor[c(TRUE, FALSE, FALSE)]
```
:::

K výběru **lze samozřejmě použít i ostatní operátory**.

```{r}
#vyber ty hodnoty ktere jsou presne 8797
vektor[vektor == 8797]

#vyber hodnoty ktere jsou vetsi nez 600 a zaroven mensi nez 50 000
vektor[vektor > 600 & vektor < 50000]
```
::::

------------------------------------------------------------------------

Vybírání specifických hodnot nemusíme ale používat pouze k jejich vyhledávání. **Můžeme pomocí tohoto principu i přiřazovat a přepisovat jednotlivé hodnoty**.

```{webr-r}
x1 <- 1:20; x1
length(x1) #kolik hodnot je ulozeno v x1?
```

Pokud chceme, aby 2. hodnota `x1` byla 55, jak ji můžeme přidat?

```{webr-r}
x1[2] <- 55; x1
```

Co když ale chceme, aby číslo 55 bylo na pozici 42?

```{webr-r}
x1[42] #v x1 v tuto chvili zadna hodnota na pozici 42 neni
x1[42] <- 55; x1
```

------------------------------------------------------------------------

### Procvičení

1.  Vyber z následujícího vektoru poslední čtyři hodnoty

    ```{webr-r}
    x <- c(15, 87, 23, 91, 42, 68, 54, 78, 54, 45, 10, 98, 54, 98)
    ```

2.  Vyber všechny hodnoty z vektoru krom té se jménem `ageGroup`. Nevypisuj, použij k výběru funkce či operátory.

```{webr-r}
c(gender = NA, ageGroup = NA, activeVoters = NA,
  country = "CZ", region = "Ostravsko", city = "Havířov")
```

------------------------------------------------------------------------

### Samostatné cvičení

```{r}
#| echo: false
countdown::countdown(minutes = 10,
                     seconds = 0,
                     start_immediately = FALSE,
                     warn_when = 120,
                     top = "0")
```

1.  Vyber z následujícího vektoru každou pátou hodnotu

    ```{r}
    seq(15.2, 45.7, 0.56)
    ```

2.  Vyber z čísel 1878 až 2324 pouze ty, které jsou dělitelné třemi. Kolik jich je?

------------------------------------------------------------------------

#### Řešení úlohy 1)

Vyber z následujícího vektoru každou pátou hodnotu.

::: fragment
1.  Pojmenujte si vektor pro snažší manipulaci.

    ```{r}
    hodnoty <- seq(15.2, 45.7, 0.56); hodnoty
    ```
:::

::: fragment
2.  Vytvořte sekvenci, která v sobě bude obsahovat čísla v intervalu pěti od 0 do posledního čísla vektoru = každé páté číslo.

    ```{r}
    seq(0, length(hodnoty), by = 5)
    ```
:::

::: fragment
3.  Použijte výslednou sekvenci čísel k indexaci.

    ```{r}
    hodnoty[seq(0, length(hodnoty), by = 5)]
    ```
:::

------------------------------------------------------------------------

#### Řešení úlohy 2)

Vyber z čísel 1878 až 2324 pouze ty, které jsou dělitelné třemi. Kolik jich je?

::: fragment
1.  Pro jednodušší zacházení s čísly si je uložíme jako proměnnou.

    ```{r}
    cisla <- 1878:2324
    ```
:::

::: fragment
2.  Co znamená, že je číslo dělitelné třemi?

    Po dělení třemi po něm nezůstane žádný zbytek. Jak toto v R zjistíme?

    ```{r}
    cisla %% 3
    ```
:::

::: fragment
3.  Chceme vybrat pouze ta čísla, která jsou dělitelná třemi = nemají žádný zbytek = zbytek je 0. Jak to provedeme?

    ```{r}
    cisla %% 3 == 0
    ```
:::

::: fragment
4.  Máme v tuto chvíli sekvenci TRUE/FALSE hodnot, které popisují vlastnosti jednotivých hodnot ve vektoru `cisla`. Jak ji využijeme k indexaci?

    ```{r}
    cisla[cisla %% 3 == 0]
    ```
:::

::: fragment
5.  Vybrali jsme v tuto chvíli všechny čísla z 1878 až 2324, které jsou dělitelné třemi. Jak zjistíme, kolik jich je?

    ```{r}
    length(cisla[cisla %% 3 == 0]) #149
    ```
:::

::: fragment
Výpočet samozřejmě můžeme provést i bez vytváření proměnných.

```{r}
length((1878:2324)[1878:2324 %% 3 == 0])
#pozor! pri tomto postupu je treba dat sekvenci do zavorky
  #r nemilosrdne dela vyber hned z nejblizsi hodnoty
  #u hranate zavorky
```
:::

------------------------------------------------------------------------

#### Matice

Matice narozdíl od vektoru mají již dvě dimenze; řádky a sloupce. Pro výběr nám tak už nestačí pouze jedno číslo.

Jak obě dimenze do výběru zapíšu? Vzpomeňte si na přehled ve [Vyhledávání pomocí \[ \]](#vyhledávání-pomocí)

```{r}
matice <- matrix(10:33, nrow = 4); matice

matice[1,3] #prvni radek treti sloupec
matice[2,5] #druhy radek, paty sloupec
matice[ ,1] #vsechny hodnoty v prvnim sloupci
matice[2, ] #vsechny hodnoty v druhem radku
matice[1:3,5:6] #prvni az treti radek a paty az sesty sloupec
```

------------------------------------------------------------------------

Můžeme používat i jména sloupců a řádků?

```{r}
dim(matice) #4 radky 6 sloupcu
dimnames(matice) <- list(LETTERS[1:4], letters[1:6])
#LETTERS a letters jsou v baseR ulozene objekty
matice

matice["A", "c"] #radek A, sloupec c
matice["B", 3] #radek B, treti sloupec
matice[,c("a", "c", "d")] #vsechny radky a sloupce a, c, d
```

**Při výběru hodnot z matice používáme totožné principy jako při výběru z vektorů; jen se musí pro výběr definovat více dimenzí.**

------------------------------------------------------------------------

#### Výběr podle specifických hodnot

Stejným způsobem, jako můžeme vybírat specifické hodnoty u vektoru, totéž můžeme provést i u matice.

```{r}
matice #zobrazeni pro prehled
#vyber vsechny radky, ktere maji v druhem sloupci vetsi hodnotu nez 15
matice[matice[,2] > 15,]
```

Z kódu je zřejmé, že budeme vybírat všechny sloupce. Ale co znamená `matice[,2] > 15` v řádcích?

```{r}
matice[,2] #vyber hodnoty vsech radku, druhy sloupec
matice[,2] > 15 #ktere z techto radku maji hodnotu vyssi jak 15?
```

Pomocí tohoto kódu si vybereme pouze ty řádky, které splňují stanovenou podmínku.

```{r}
matice[matice[,2] > 15,]
matice[c("C", "D"),] #totozny vysledek
```

::: callout-important
## Třeba jasně definovat podmínky pro všechny sloupce a řádky

Přemýšlejte nad tím takto: musí R vždy říct, jestli tam má každý sloupec či řádek zařadit; zda má být součástí výběru (TRUE)

**Ukázka**

```{r}
matice[c("C", "D"),]
matice[c(FALSE, FALSE, TRUE, TRUE),]
```
:::

::: fragment
Proč nemůžeme jednoduše použít následující kód?

```{webr-r}
#| context: setup
matice <- matrix(10:33, nrow = 4)
dimnames(matice) <- list(LETTERS[1:4], letters[1:6])
```

```{webr-r}
matice["B" > 15, ]
```
:::

::: fragment
Huh, proč se nám vybrala celá matice?
:::

::: fragment
```{r}
"B" > 15 #porovnavame zda je text vetsi nez pismeno
```
:::

::: fragment
1.  Dojde k nucené konverzi do charakteru, aka

    ```{r}
    "B" > "15"
    ```

    Proč ale?
:::

::: fragment
2.  Jednotlivá písmena jsou v R uložena podle abecedy; B je tedy tak na druhé pozici.

    ```{r}
    "2" > "15"
    ```

Jak to že je ale 2 \> 15?
:::

::: fragment
3.  Neporovnáváme čísla, ale písmena - R hodnotí text "15" na základě prvního symbolu. A `2 > 1 = TRUE`
:::

::: fragment
4.  Vznikne nám proto defakto následující pokyn:

    ```{r}
    matice[TRUE, ]
    ```

    Pro to, aby výběr matice fungoval, tak je potřeba, aby se definovaly všechny řádky. Zde jsme ale dodali pouze jednu hodnotu. Víme ale, že R si v některých případech dokáže hodnoty samo doplnit - viz \[Automatické generování hodnot\]
:::

::: fragment
5.  Ve výsledku jsme R zadali následující kód:

    ```{r}
    matice[c(TRUE, TRUE, TRUE, TRUE), ] #aka vyber vsechny radky
    ```
:::

::: fragment
**Proč vám toto celé říkám?**

- Nemůžete počítat s tím, že když něco "pokazíte", nutně vám vyskočí error.

- R má spoustu funkcí a pokynů, i já někdy narazím na mně zcela nové věci

- **Kontrolujte si jednotlivé pokyny, které skládáte dohromady!**
:::

------------------------------------------------------------------------

### Procvičení

1.  Vyber z proměnné `matice` všechny hodnoty vyšší jak 30.

    ```{webr-r}

    ```

2.  Vyber z prvního řádku `matice` ty sloupce, které mají hodnoty větší jak 25 nebo menší jak 15.

    ```{webr-r}

    ```

3.  Vyber ty řádky `matice`, které mají ve sloupci `c` hodnotu přesně 20.

    ```{webr-r}

    ```

4.  Vyber každý druhý sloupec, ale pouze ty řádky které začínají na liché hodnoty.

    ```{webr-r}

    ```

------------------------------------------------------------------------

#### Data frame

Data frame a matice mají stejný počet dimenzí; **data frame tak můžeme indexovat stejným způsobem jako matici.**

```{r}
vek <- c(27,35,30,47,42)
sex <- c("female","male","male","female","female")
strana <-c("sin","sin","dx","dx","dx")
ID <- c("SC_001", "SC_007", "SC_013", "SC_014", "SC_020")
vzorek <- data.frame(vek, strana, sex, row.names = ID); vzorek

vzorek[1, 3] #prvni radek treti sloupec
vzorek["SC_020", 1] #radek jmenem SC_020 prvni sloupec
vzorek[vzorek[,2] == "sin",] #radky ktere maji v druhem sloupci hodnotu "sin" a vsechny sloupce
```

------------------------------------------------------------------------

::: callout-important
## Výběr hodnot specifického sloupce

Data frame má narozdíl od matice ovšem jednu velkou výhodu; můžeme indexovat přímo pomocí jmen sloupců. Co to znamená?

```{r}
#vyber sloupec strana
vzorek["strana"]
#vyber vsechny hodnoty ve sloupci strana 
vzorek$strana
```
:::

Při vytváření indexování specificky pojmenovaného sloupce se někdy používá symbol \$.

> **symbol `$` provede výběr hodnot ze specifické komponenty datové struktury**

Proč tento symbol nelze použít i u matice? *Nápověda: jaká je definice data frame a matice?*

:::: fragment
Zatímco matice je defakto jeden dlouhý vektor poskládaný ve dvou dimenzích, **data frame je objekt složený z jiných objektů**.

```{webr-r}
matice
matice$a

```

::: fragment
```{r}
str(matice) #jaka je struktura matice "matice"
str(vzorek) #jaka je struktura data frame "vzorek"
```
:::
::::

------------------------------------------------------------------------

#### Rozdíl mezi \$ a \["název sloupce"\]

Jak bylo vidět, při vybírání specifického sloupce můžeme využívat dva způsoby - \$ a \[ \]. Jaký je mezi nimi ale rozdíl?

- **\$**

```{r}
vzorek$vek #vyber hodnoty ze sloupce vek
mean(vzorek$vek) #jaka je aritmeticky prumerna hodnota vek?
```

- **\[ \]**

```{webr-r}
mean(vzorek["vek"])
```

::: fragment
Proč nám není umožněno tuto funkci spustit? *Nápověda: jaká je datová chrakteristika tohoto výběru?*
:::

::: fragment
```{r}
class(vzorek$vek)
class(vzorek["vek"])
```

Můžeme počítat aritmetický průměr z číselných hodnot; rozhodně neuděláme průměr z "data frame".
:::

------------------------------------------------------------------------

##### Kdy použijeme výběr pomocí \$?

Kdykoliv, kdy potřebujeme **pracovat s čistými hodnotami, nikoliv celou datovou strukturou**. Příkladem může být buď přepisování, nebo třeba i doplňování dalších hodnot.

```{r}
doctor <- list(name = c("William Hartnell","Patrick Troughton","Jon Pertwee","Tom Baker","Peter Davison","Colin Baker","Sylvester McCoy","Paul McGann", "John Hurt","Christopher Eccleston"),
               year = c(55,46,50,40,29,40,44,36,73,41), 
               episodes = c("134","119","128","172","69","31","42","1","2","13")); doctor

#pridame nove hodnoty - musime prepsat a pridat
doctor <- list(name = c(doctor$name,"David Tennat","Matt Smith","Peter Capaldi", "Jodie Whittaker"),
               year = c(doctor$year,34,27,55,36),
               episodes = c(doctor$episodes,"47","44","39","10")); doctor
```

------------------------------------------------------------------------

#### Můžu si takto zavolat i řádky?

R automaticky usuzuje že data frame je poskládaný tak, že jednotlivá měření/vzorky/pozorování jsou zapsaná v řádcích, nikoliv ve sloupcích - jedná se o takzvaný **long formát dat**. To znamená, že usuzuje, že jednotlivé vektory, ze kterých se data frame skládá, budou sloupce.

```{webr-r}
vzorek["SC_001"] #v data frame neexistuje neco jako objekt SC_001
```

------------------------------------------------------------------------

### Procvičení

::: callout-tip
## Zkušební datasety v R

V R a nahraném balíčku `dplyr` je nainstalováno několik datasetů určených k zkoušení si funkcí a celkovému hraní si. Datasety `iris` a `storms`, které budeme v následujícím procvičení používat, jsou právě jedněmi z nich. Pro jejich používání není potřeba je jakkoliv nahrávat či ukládat jako proměnné.
:::

1.  Jak průměrně silné byly větry (`wind`) v roce, kdy jste se narodili (`year`)? Použijte dataset `dplyr::storms`.

```{webr-r}

```

2.  Vyber všechny bouře, které v roce vašeho narození měly hodnoty větru větší či menší než směrodatná odchylka. Kolik jich bylo?

```{webr-r}

```

3.  Vyber z satasetu `iris` všechny měření, kdy délka kalichu květiny (`Sepal.Lenght`) je větší než 5, nebo jejich šířka (`Sepal.Width`) je větší než 3.5 a zároveň se jedná o druh (`Species`) setosa.

```{webr-r}

```

3.  Jaká je průměrná délka okvětních lístků (`Petal.Lenght`) pro druh setosa, poté co jsme provedli filtrování v předchozím cvičení?

```{webr-r}

```

------------------------------------------------------------------------

## Samostatné cvičení

```{r}
#| echo: false
countdown::countdown(minutes = 15,
                     seconds = 0,
                     start_immediately = FALSE,
                     warn_when = 120,
                     top = "0")
```

**Nahrajte si data uložená v souboru `bikes.RData`.** Ten obsahuje přehled testování motorek, kdy každý stroj dostal určitý počet bodů za hodnocení rychlosti (`speed`), pohodlnosti na vzdálenost (`dist`) a pak ještě celkové hodnocení od nezávislého odbnorníka (`rating`), kdy 1 je nejlepší, 5 je nejhorší.

1.  Vyberte motorky, které měli počet bodů z obou hodnocení dohromady sice menší než 50, ale i tak dostaly rating lepší než 3. Vypište jejich názvy.

2.  Přepiš hodnoty ve sloupci `distance` v tomto výběru tak, že pokud je hodnota menší jak 20, bude nahrazena slovy "pod dvacet", pokud bude větší nebo rovno 20, bude nahrazena slovy "dvacet +".

**Nahrajte si data uložená v souboru `inventory.RData`.** Ten obsahuje data prodejnosti jednotlivých položek v obchodě během měsíční akce, specificky jaká byla jejich původní cena v Kč (`price`), o kolik byl výrobek zlevněn (`discount`) a kolik kusů se během akce prodalo (`sales`).

3.  Spočítejte, kterých položek bylo během akce prodáno za více než 100.000 Kč.

4.  Vyberte ty položky, které byly zlevněny o více než 85 procent.

------------------------------------------------------------------------

#### Řešení úlohy 1)

Vyberte motorky, které měli počet bodů z obou hodnocení dohromady sice menší než 50, ale i tak dostaly rating lepší než 3. Vypište jejich názvy.

::: fragment
1.  Podíváme se na strukturu datového objektu `bikes` a necháme si vyjet prvních několik řádků. Vidíme, že se jedná o data frame, takže k vyhledávání můžeme používat přímo názvy sloupců.

    ```{r}
    #| label: setup
    #| include: false
    load("bikes.RData")
    load("inventory.Rdata")
    ```

    ```{r}
    str(bikes)
    head(bikes)
    ```
:::

::: fragment
2.  Vyber ty řádky, kde součet z hodnot ve sloupcích `speed` a `dist` je \< 50, a zároveň hodnota ve sloupci `rating` je \< 3.

    ```{r}
    bikes[bikes$speed + bikes$dist < 50 & bikes$rating < 3,]
    ```
:::

::: fragment
3.  Pro snažší práci s tímto výběrem si ho pojmenujeme a uložíme.

    ```{r}
    divneHodnoceni <- bikes[bikes$speed + bikes$dist < 50 & bikes$rating < 3,]
    ```
:::

::: fragment
4.  Vypíšeme si názvy řádků tohoto výběru.

    ```{r}
    rownames(divneHodnoceni)
    ```
:::

------------------------------------------------------------------------

#### Řešení úlohy 2)

Přepiš hodnoty ve sloupci `dist` v tomto výběru tak, že pokud je hodnota menší jak 20, bude nahrazena slovy "pod dvacet", pokud bude větší nebo rovno 20, bude nahrazena slovy "dvacet +".

::: fragment
1.  Vybereme hodnoty ze sloupce `dist` které jsou \< 20 a přepíšeme je na charaktery "pod dvacet".

    ```{r}
    divneHodnoceni$dist[divneHodnoceni$dist < 20] <- "pod dvacet"
    ```
:::

::: fragment
2.  Podíváme se na to, jaká je nyní struktura sloupce `dist`.

    ```{r}
    str(divneHodnoceni$dist)
    ```
:::

::: fragment
3.  Hodnoty se nuceně převedly na charaktery; není možné mít v jednom vektoru numeric a character hodnoty. Proto nemůžeme použít operátory \> nebo \<, ale použijeme místo toho operátory != aka "není přesně".

    ```{r}
    divneHodnoceni$dist[divneHodnoceni$dist != "pod dvacet"] <- "dvacet +"
    divneHodnoceni
    ```
:::

------------------------------------------------------------------------

#### Řešení úlohy 3)

Spočítejte, které položky byly během akce prodány za více než 100.000 Kč.

::: fragment
1.  Podíváme se na strukturu datového objektu `inventory` a necháme si vyjet prvních několik řádků. Vidíme, že objekt je matice - musíme se proto při výběru sloupců řídit podle podmínek pro matici.

    ```{r}
    str(bikes)
    head(bikes)
    ```
:::

::: fragment
2.  Zjistíme si ceny jednotlivých položek během akce. Provedeme to tak, že vynásobíme hodnoty ve sloupci pro původní cenu `price` s hodnotami ve slevovém sloupci `discount`. Nezapomeňte, že pro výpočet si musíte informaci o slevě převést do správných hodnot. Pro snadnější následující práci si výsledek pojmenujme.

    ```{r}
    discountedPrice <- inventory[,"price"] * (inventory[,"discount"]/100)
    ```
:::

::: fragment
3.  Spočítáme si zisk u každé položky, a to tak, že vynásobíme cenu během akce (`discontedPrice`) s hodnotami ze sloupce `sales`. Pro snadnější následující práci si výsledek pojmenujme.

    ```{r}
    revenue <- discountedPrice * inventory[,"sales"]
    ```
:::

::: fragment
4.  Vypíšeme si všechny položky, které vydělaly více jak 100.000 Kč.

    ```{r}
       revenue[revenue > 100000]
    ```
:::

------------------------------------------------------------------------

#### Řešení úlohy 4)

Vyberte ty položky, které byly zlevněny o více než 85 procent.

::: fragment
1.  Vybereme všechny sloupce z matice a pouze ty řádky, které mají ve sloupci `discount` hodnoty \>= 85.

    ```{r}
    inventory[inventory[,"discount"] >= 85,]
    ```
:::

------------------------------------------------------------------------

## Nahrávání dat

### base R

#### header

#### sep

#### dec

```{r}
dataTxt <- read.delim(file = "intro_data.txt",
                       header = TRUE,
                       sep = "",
                       dec = ".",
                       na.strings = "NA")
```

```{r}
dataTxt2 <- read.delim(file = "intro_data.txt")
```

```{r}
identical(dataTxt, dataTxt2)
```

Proč?

Podívejme se do nápovědy pro tuto funkci.

> **série za sebou jdoucích hodnot totožného datového typu**

- vektor o jedné hodnotě je někdy nazýván *atomický vektor* či *skalár*

- jednodimenzionální (důležité hlavně při vyhledávání)

- při použití více datových typů dochází k nucené konverzi do jednotného typu

```{r}
seq(1, 10, 2)
```

------------------------------------------------------------------------

### Balíček `readr`

- je v tidyverse

  - rozestup vždy po jedné

```{r}
-5:3
0.65:9.18
```

- funkce `c` spojí hodnoty jakéhokoliv typu do vektoru

------------------------------------------------------------------------

### Balíček `readxl` - ach ty Excely...

- je v tidyverse

ff

![](images/Nový%20projekt.png){fig-align="center"}

------------------------------------------------------------------------

### Samostatné cvičení

```{r}
#| echo: false
countdown::countdown(minutes = 5,
                     seconds = 0,
                     start_immediately = FALSE,
                     warn_when = 120,
                     top = "0")
```

------------------------------------------------------------------------

## Příprava na další hodinu

Nainstalujte si balíček [`tidyverse`](https://tidyverse.org/packages/). Počítejte s tím, že balíček se bude stahovat a instalovat klidně i **několik desítek minut** - záleží na vašem počítači.

## Domácí úkol

1.  Vyberte z čísel 1878 až 2324 pouze ty, které jsou dělitelné třemi.

2.  Vytvořte list, který bude obsahovat informace o vašem oblíbeném filmu; jeho název, kdo jej režiséroval, kdy byl vydán, alespoň tři jména herců a hereček, kteří v něm hráli, na jaké ceny byl nominován a jaké společnosti se angažovaly jako jeho producenti. Jakmile budete mít list vytvořený, připojte do seznamu herců/hereček Zaphoda Beeblebroxe.

3.  Z datasetu `dplyr::starwars` vyberte všechny charaktery, u kterých máme hodnoty ve všech sloupcích (žádné NA), které pochází z Tatooine a nejsou to Droidi. Nápověda: k výběru budete muset použít funkci. Vyhledejte si na internetu (nikoliv AI) alespoň dvě funkce, které můžete využít k vyyřešení tohoto příkladu. Váš výsledek by měl vypadat následovně:

```{webr-r}
#| context: output
starwars <- na.omit(dplyr::starwars)
starwars$name[starwars$homeworld == "Tatooine" & starwars$species != "Droid"]
```

Skript s výpočtem zašlete nejpozději do pátku před příští hodinou na [lucie.hoskova\@ruk.cuni.cz](mailto:lucie.hoskova@ruk.cuni.cz)
